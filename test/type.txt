# Type - Field with type

SomeModel
  field: int

==>

root(Model(ModelName, Field(FieldName, Type(TypeSymbol))))

# Type - Field with enum type

SomeModel
  field: BLUE, GREEN

==>

root(Model(ModelName, Field(FieldName, Type(TypeEnumVariant,TypeEnumVariant))))

# Type - Field with union type

SomeModel
  field: int | BLUE, GREEN

==>

root(Model(ModelName, Field(FieldName, Type(TypeSymbol, TypePipe, TypeEnumVariant, TypeEnumVariant))))

# Type - Field with complex type

SomeModel
  field: BLUE, GREEN | int | [string | date ]

==>

root(Model(ModelName, Field(FieldName,
  Type(TypeEnumVariant, TypeEnumVariant, TypePipe, TypeSymbol, TypePipe, TypeBracketOpen, TypeSymbol, TypePipe, TypeSymbol, TypeBracketClose)
)))

# Type - Field with contextual types

SomeModel
  field: date
    (js): string
    (db): int

==>

root(Model(ModelName, Field(
    FieldName,
    Type(TypeSymbol),
    ParenOpen, TypeContext, ParenClose, Type(TypeSymbol),
    ParenOpen, TypeContext, ParenClose, Type(TypeSymbol)
)))

# Type alias

:Mytype = int | string

==>

root(TypeAlias(TypeAliasName, Equal, Type(TypeSymbol, TypePipe, TypeSymbol)))

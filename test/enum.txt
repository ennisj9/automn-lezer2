# Empty Enum

|Color

==>

root(Enum(EnumName))

# Enum with variants

|Color
  BLUE
  GREEN

==>

root(Enum(EnumName, Variant(VariantName), Variant(VariantName)))

# Enum with describers

|Color
  : int
  [tag]
  .version = 1
  > single doc
  >>
    multiline doc
  BLUE
  GREEN

==>

root(Enum(
    EnumName,
    Type(TypeSymbol),
    BracketOpen, Tag, BracketClose,
    Period, PropertyKey, Equal, Number,
    SingleDocumentationCaret, DocumentationString,
    DoubleCaret,
    DocumentationString,
    Variant(VariantName),
    Variant(VariantName)
))

# Enum variants with payloads

|MaybeBoolInt
  Some(bool, int)
  None

==>

root(Enum(EnumName,
  Variant(VariantName,ParenOpen,TypeSymbol, TypeSymbol,ParenClose),
  Variant(VariantName)
))

# Enum variants with values

|Gender
  MALE = "M"
  FEMALE = "F"
  OTHER = "O"

==>

root(Enum(EnumName,
  Variant(VariantName,VariantEqual,ValueString),
  Variant(VariantName,VariantEqual,ValueString),
  Variant(VariantName,VariantEqual,ValueString),
))

# Enum with variant describers

|Color
  BLUE
    = "BLUE"
    [tag]
    .version = 1
    > single doc
    >>
      multiline doc
  GREEN

==>

root(Enum(
    EnumName,
    Variant(VariantName,VariantEqual,ValueString,BracketOpen, Tag, BracketClose, Period, PropertyKey, Equal, Number, SingleDocumentationCaret, DocumentationString, DoubleCaret, DocumentationString), Variant(VariantName)
))
